import React from "react";
import ReactDOM from "react-dom";
import Greeting from "./App";

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<Greeting name="mukesh" gender="Male"/>);
